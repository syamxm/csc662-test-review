services:
  app:
    build: .
    container_name: csc662-test-review
    restart: unless-stopped
    environment:
      PORT: 8662
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
